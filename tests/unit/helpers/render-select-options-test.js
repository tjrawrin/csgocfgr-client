import { renderSelectOptions } from 'csgocfgr/helpers/render-select-options';
import { module, test } from 'qunit';

module('Unit | Helper | render select options');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = renderSelectOptions([42]);
  assert.ok(result);
});
